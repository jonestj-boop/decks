**Card** — content surface, soft-square corners, neutral shadow. Never a colored left-border.

```jsx
<Card variant="raised" interactive>…</Card>
<Card variant="dark">…</Card>
```

`variant`: raised | flat | subtle | dark. `interactive` lifts on hover. `padding`: none | sm | md | lg.

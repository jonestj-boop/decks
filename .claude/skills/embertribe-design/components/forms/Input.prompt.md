**Input** — labeled text field with uppercase label, 2px border, red focus ring.

```jsx
<Input label="Work email" placeholder="you@company.com" />
<Input label="Budget" error="Required" />
```

Props: `label`, `hint`, `error`, plus native input attributes.

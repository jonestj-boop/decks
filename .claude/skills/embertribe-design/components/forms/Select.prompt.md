**Select** — native dropdown with brand styling and custom chevron.

```jsx
<Select label="Monthly budget" options={["< $10k", "$10k–50k", "$50k+"]} />
```

Props: `label`, `hint`, `error`, `options` (string[] or {value,label}[]), or `<option>` children.
